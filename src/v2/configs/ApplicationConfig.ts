const DEFAULT_PORT: number = 5000;

const EXPOSED_HEADERS: string[] = [
  'authorization',
];

const TRUST_PROXY: number = 1;

const IS_CIRCULAR_CHECK_ENABLED: boolean = true;

export default {
  DEFAULT_PORT,
  EXPOSED_HEADERS,
  TRUST_PROXY,
  IS_CIRCULAR_CHECK_ENABLED,
};
